{
  "name": "lambda-handler",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts2": {
     "prebuild": "del /q dist",
     "copy:yaml": "copyfiles -f collector.yaml dist",
     "build": "esbuild index.js --bundle  --sourcemap --platform=node --target=es2020 --outfile=dist/index.js",
     "postbuild": "npm run copy:yaml && cd dist && 7z a -tzip index.zip index.js*"
  },
  "dependencies": {
    "axios": "^1.15.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.161",
    "esbuild": "^0.28.0"
  }
}
